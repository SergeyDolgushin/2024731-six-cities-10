const idForTest = 'Properties galleery image';

const imagesForTest = [
  'image',
  'image',
  'image',
  'image',
  'image',
  'image',
];

const titleForTest = 'title';

export { idForTest, imagesForTest, titleForTest };
